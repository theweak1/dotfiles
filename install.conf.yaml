- defaults:
    link:
      relink: true

- clean: ['~']

- link:
#    ~/.config/tmux/tmux.conf: tmux.conf
     ~/.bashrc: 
     ~/.gitconfig:
#    ~/.vim: vim
#    ~/.vimrc: vimrc

- create:
    - ~/Work

- shell:
#  - [git submodule update --init --recursive, Installing submodules]
   - echo "hey you guys"
   - sleep 2
   - 
     command: ./test.bash
     stdout: true
     stderr: true
