- defaults:
    link:
      relink: true

- clean: ['~']

# - link:
#    ~/.tmux.conf: tmux.conf
#    ~/.vim: vim
#    ~/.vimrc: vimrc

- create:
    - ~/Work
    - ~/hye/you/guys

# - shell:
#  - [git submodule update --init --recursive, Installing submodules]
